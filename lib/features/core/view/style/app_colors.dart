import 'dart:ui';

// TODO: make the colors mare fitting and check all contrasts
class AppColors {
  static const background = Color(0xff000000);
  static const foreground = Color(0xffffffff);
  static const primary = Color(0xffffc107);
  static const secondary = Color(0xff701cff);
  static const positive = Color.fromARGB(255, 149, 255, 28);
  static const negative = Color.fromARGB(255, 255, 111, 28);

  static const onBackground = Color(0xffeeeeee);
  static const onForeground = Color(0xff222222);
  static const onPrimary = background;
  static const onSecondary = background;
  static const onPositive = background;
  static const onNegative = background;
}
